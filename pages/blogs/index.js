const BlogsPage = () => {
    return (
      <h2>BlogsPage</h2>
    )
  }
  
  export default BlogsPage
  
  /**
   * 1. blogs(slug)
   * 2. Link from next/link, instead of normal anchor
   * 3. Different ways of setting up Link Tag apart from href (To-do)
   * 4. Navigation to the page on click of button. (To-do)
   */